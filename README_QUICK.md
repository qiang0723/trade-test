# 快速操作指南

## ⚡ 为什么需要手动执行

通过AI助手执行以下命令时会触发**权限请求**，等待批准会导致卡顿：
- ❌ Git操作（add, commit, push）
- ❌ 网络操作（启动Web服务、Docker）
- ❌ 系统级操作（安装依赖、后台进程）

**解决方案**：直接在终端手动执行，快速无卡顿！

---

## 🚀 快速启动脚本

### 1. 启动服务
```bash
./quick-start.sh
```
**功能**：
- 自动检查依赖
- 选择启动方式（直接/Docker）
- 无需等待权限批准

---

### 2. Git提交
```bash
./quick-commit.sh
```
**功能**：
- 显示当前修改
- 交互式提交
- 可选推送到远程

---

### 3. 运行测试
```bash
./quick-test.sh
```
**功能**：
- 选择测试范围
- 运行特定测试
- 查看测试结果

---

### 4. PR-DUAL测试（双周期独立结论）
```bash
./quick-test-dual.sh
```
**功能**：
- 验证双周期功能
- 检查配置和API
- 快速集成测试

---

## 📋 常用命令速查

### 服务管理
```bash
# 直接启动
python3 btc_web_app_l1.py

# 后台启动
nohup python3 btc_web_app_l1.py > logs/web.log 2>&1 &

# Docker启动
docker compose -f docker-compose-l1.yml up -d

# 查看Docker日志
docker logs -f l1-advisory-layer

# 停止Docker服务
docker compose -f docker-compose-l1.yml down
```

### Git操作
```bash
# 查看状态
git status

# 快速提交
git add . && git commit -m "update" && git push

# 查看日志
git log --oneline -10
```

### 测试运行
```bash
# 所有测试
python3 tests/run_all_tests.py

# P0测试
python3 -m pytest tests/test_p0_*.py -v

# 单个测试
python3 -m pytest tests/test_p0_volume_unit_bugfix.py -v
```

---

## 🔧 访问地址

| 启动方式 | 地址 | 端口 | 说明 |
|---------|------|------|------|
| 直接启动 | http://localhost:5001 | 5001 | 单一决策页面 |
| Docker启动 | http://localhost:8001 | 8001 | 单一决策页面 |
| 双周期页面 | http://localhost:5001/dual | 5001 | PR-DUAL双周期决策 |
| 双周期页面 | http://localhost:8001/dual | 8001 | PR-DUAL双周期决策 |

---

## 💡 提示

1. **首次使用**：先运行 `./quick-start.sh` 检查环境
2. **开发调试**：使用直接启动（可看实时日志）
3. **生产部署**：使用Docker启动（稳定可靠）
4. **代码提交**：使用 `./quick-commit.sh`（交互式）
5. **测试验证**：使用 `./quick-test.sh`（灵活选择）

---

## 🐛 问题排查

### 服务启动失败
```bash
# 检查端口占用
lsof -i :5001

# 查看详细错误
python3 btc_web_app_l1.py

# Docker查看日志
docker logs l1-advisory-layer
```

### Git推送失败
```bash
# 检查远程分支
git remote -v

# 拉取最新代码
git pull --rebase

# 强制推送（谨慎）
git push -f
```

---

**记住**：遇到卡顿，直接在终端执行！
