# 💾 数据库持久化存储配置说明

> 最后更新：2026-01-20  
> 适用版本：v3.0+

---

## 📋 目录

1. [概述](#概述)
2. [持久化方案](#持久化方案)
3. [配置说明](#配置说明)
4. [目录结构](#目录结构)
5. [使用方法](#使用方法)
6. [数据备份](#数据备份)
7. [数据恢复](#数据恢复)
8. [故障排查](#故障排查)

---

## 📖 概述

### 为什么需要持久化？

在Docker容器中，默认情况下数据存储在容器内部。当容器被删除或重建时，所有数据都会丢失。

**持久化存储的优势：**
- ✅ **数据安全**：容器重启、重建、更新时数据不丢失
- ✅ **数据备份**：可以直接备份宿主机上的数据库文件
- ✅ **数据分析**：可以使用SQLite客户端直接查看数据
- ✅ **数据迁移**：方便在不同环境间迁移数据

### 涉及的数据库

项目使用两个SQLite数据库：

| 数据库文件 | 用途 | 大小（典型） |
|-----------|------|-------------|
| `market_state.db` | 状态机状态持久化 | ~10-20KB |
| `market_signals.db` | 历史信号记录 | ~1-10MB |

---

## 🔧 持久化方案

### 技术方案

使用 **Docker Volume 挂载** 将容器内的数据库目录映射到宿主机。

```
容器内路径：/app/db/
    ↓ (挂载)
宿主机路径：./data/db/
```

### 映射关系

| 容器内 | 宿主机 | 说明 |
|--------|--------|------|
| `/app/db/market_state.db` | `./data/db/market_state.db` | 状态机状态 |
| `/app/db/market_signals.db` | `./data/db/market_signals.db` | 历史信号 |

---

## ⚙️ 配置说明

### 1. Docker Compose 配置

**文件：`docker-compose.yml`**

```yaml
services:
  trade-info:
    # ... 其他配置 ...
    volumes:
      # 数据目录挂载
      - ./btc_market_data:/app/btc_market_data
      # 数据库目录挂载（持久化）
      - ./data/db:/app/db
```

### 2. Python 代码配置

#### **database.py**

```python
def get_signal_db():
    """获取全局数据库实例"""
    global _db_instance
    if _db_instance is None:
        import os
        # 数据库存储在 /app/db 目录（Docker挂载点）
        db_dir = os.getenv('DB_DIR', '/app/db')
        os.makedirs(db_dir, exist_ok=True)
        db_path = os.path.join(db_dir, 'market_signals.db')
        _db_instance = SignalDatabase(db_path=db_path)
    return _db_instance
```

#### **market_state_machine.py**

```python
def get_state_machine():
    """获取全局状态机实例"""
    global _state_machine_instance
    if _state_machine_instance is None:
        import os
        # 数据库存储在 /app/db 目录（Docker挂载点）
        db_dir = os.getenv('DB_DIR', '/app/db')
        os.makedirs(db_dir, exist_ok=True)
        db_path = os.path.join(db_dir, 'market_state.db')
        storage = StateStorage(db_path=db_path)
        _state_machine_instance = MarketStateMachine(storage=storage)
    return _state_machine_instance
```

### 3. 环境变量（可选）

可以通过环境变量自定义数据库目录：

```yaml
# docker-compose.yml
services:
  trade-info:
    environment:
      - DB_DIR=/app/db  # 默认值，可以修改
```

---

## 📁 目录结构

### 项目目录

```
trade-info/
├── data/                      # 数据持久化根目录
│   └── db/                    # 数据库文件目录
│       ├── .gitkeep           # Git占位文件
│       ├── README.md          # 目录说明
│       ├── market_state.db    # 状态机数据库（运行时生成）
│       └── market_signals.db  # 信号记录数据库（运行时生成）
│
├── docker-compose.yml         # Docker配置（含volume挂载）
├── database.py                # 信号数据库模块
└── market_state_machine.py    # 状态机模块
```

### Git 版本控制

**提交到Git：**
- ✅ `data/db/.gitkeep` - 保留目录结构
- ✅ `data/db/README.md` - 目录说明

**忽略（.gitignore）：**
- ❌ `data/db/*.db` - 数据库文件
- ❌ `data/db/*.db-*` - 数据库临时文件（journal, wal, shm）

---

## 🚀 使用方法

### 首次启动

```bash
# 1. 确保data/db目录存在
mkdir -p data/db

# 2. 启动Docker服务
docker compose up -d

# 3. 查看数据库文件（等待几秒后）
ls -lh data/db/
```

**预期输出：**
```
-rw-r--r--  1 user  staff    20K  market_signals.db
-rw-r--r--  1 user  staff    12K  market_state.db
```

### 重启服务（数据保留）

```bash
# 停止服务
docker compose down

# 重新启动
docker compose up -d

# 数据库文件仍然存在
ls -lh data/db/
```

### 查看数据库内容

#### **使用SQLite命令行**

```bash
# 进入数据库
sqlite3 data/db/market_signals.db

# 查看表结构
.schema

# 查看最近10条信号
SELECT * FROM market_signals ORDER BY timestamp DESC LIMIT 10;

# 退出
.quit
```

#### **使用Python脚本**

```python
import sqlite3

# 连接数据库
conn = sqlite3.connect('data/db/market_signals.db')
cursor = conn.cursor()

# 查询最近信号
cursor.execute("""
    SELECT timestamp, symbol, trade_action, state_reason 
    FROM market_signals 
    ORDER BY timestamp DESC 
    LIMIT 10
""")

for row in cursor.fetchall():
    print(row)

conn.close()
```

---

## 💾 数据备份

### 方案1：直接复制文件

```bash
# 备份当前数据库
cp data/db/market_signals.db data/db/market_signals_backup_$(date +%Y%m%d).db
cp data/db/market_state.db data/db/market_state_backup_$(date +%Y%m%d).db

# 或者备份整个目录
tar -czf db_backup_$(date +%Y%m%d_%H%M%S).tar.gz data/db/
```

### 方案2：使用SQLite备份命令

```bash
# 备份 market_signals.db
sqlite3 data/db/market_signals.db ".backup 'data/db/market_signals_backup.db'"

# 备份 market_state.db
sqlite3 data/db/market_state.db ".backup 'data/db/market_state_backup.db'"
```

### 方案3：导出SQL脚本

```bash
# 导出为SQL文件
sqlite3 data/db/market_signals.db .dump > market_signals_$(date +%Y%m%d).sql
sqlite3 data/db/market_state.db .dump > market_state_$(date +%Y%m%d).sql
```

### 自动备份脚本

**创建 `backup_db.sh`：**

```bash
#!/bin/bash

# 配置
BACKUP_DIR="./backups/db"
SOURCE_DIR="./data/db"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)

# 创建备份目录
mkdir -p "$BACKUP_DIR"

# 备份数据库文件
cp "$SOURCE_DIR/market_signals.db" "$BACKUP_DIR/market_signals_$TIMESTAMP.db"
cp "$SOURCE_DIR/market_state.db" "$BACKUP_DIR/market_state_$TIMESTAMP.db"

# 保留最近7天的备份
find "$BACKUP_DIR" -name "*.db" -mtime +7 -delete

echo "✅ 数据库备份完成：$TIMESTAMP"
```

**设置定时任务（crontab）：**

```bash
# 编辑crontab
crontab -e

# 添加每天凌晨2点自动备份
0 2 * * * /path/to/trade-info/backup_db.sh >> /path/to/trade-info/backup.log 2>&1
```

---

## 🔄 数据恢复

### 从备份恢复

```bash
# 1. 停止Docker服务
docker compose down

# 2. 恢复数据库文件
cp data/db/market_signals_backup_20260120.db data/db/market_signals.db
cp data/db/market_state_backup_20260120.db data/db/market_state.db

# 3. 重新启动服务
docker compose up -d
```

### 从SQL脚本恢复

```bash
# 1. 停止服务
docker compose down

# 2. 删除旧数据库
rm data/db/market_signals.db

# 3. 从SQL文件恢复
sqlite3 data/db/market_signals.db < market_signals_20260120.sql

# 4. 重新启动服务
docker compose up -d
```

### 清空数据（重置）

```bash
# 1. 停止服务
docker compose down

# 2. 删除所有数据库文件
rm data/db/*.db data/db/*.db-*

# 3. 重新启动服务（将创建新的空数据库）
docker compose up -d
```

---

## 🔍 故障排查

### 问题1：数据库文件未创建

**症状：**
```bash
ls data/db/
# 输出：只有 .gitkeep 和 README.md，没有 .db 文件
```

**原因：**
- Docker volume挂载失败
- 代码中数据库路径配置错误
- 容器没有写权限

**解决方案：**
```bash
# 1. 检查docker-compose.yml配置
cat docker-compose.yml | grep -A5 volumes

# 2. 检查容器内路径
docker exec trade-info-app ls -la /app/db/

# 3. 检查容器日志
docker logs trade-info-app | grep -i "database\|db"

# 4. 检查目录权限
ls -ld data/db/
chmod 755 data/db/
```

---

### 问题2：数据库锁定错误

**症状：**
```
database is locked
```

**原因：**
- 多个进程同时访问数据库
- SQLite WAL模式文件未清理

**解决方案：**
```bash
# 1. 停止服务
docker compose down

# 2. 清理临时文件
rm data/db/*.db-wal data/db/*.db-shm

# 3. 重新启动
docker compose up -d
```

---

### 问题3：数据库损坏

**症状：**
```
database disk image is malformed
```

**原因：**
- 容器异常终止
- 磁盘空间不足
- 文件系统错误

**解决方案：**
```bash
# 1. 尝试修复数据库
sqlite3 data/db/market_signals.db "PRAGMA integrity_check;"

# 2. 如果无法修复，从备份恢复
cp data/db/market_signals_backup.db data/db/market_signals.db

# 3. 如果没有备份，导出可恢复的数据
sqlite3 data/db/market_signals.db ".recover" > recovered.sql
```

---

### 问题4：容器重启后数据丢失

**症状：**
- 容器重启后，数据库为空
- 宿主机上有数据文件，但容器内看不到

**原因：**
- Volume挂载路径配置错误
- 容器使用了错误的数据库路径

**解决方案：**
```bash
# 1. 检查volume挂载
docker inspect trade-info-app | grep -A10 Mounts

# 2. 验证路径映射
# 宿主机
ls -la ./data/db/

# 容器内
docker exec trade-info-app ls -la /app/db/

# 3. 检查代码中的DB_DIR环境变量
docker exec trade-info-app env | grep DB_DIR
```

---

### 问题5：性能下降

**症状：**
- 数据库操作变慢
- 响应时间增加

**原因：**
- 数据库文件过大
- 缺少索引
- SQLite日志模式问题

**解决方案：**
```bash
# 1. 检查数据库大小
du -h data/db/*.db

# 2. 清理旧数据
sqlite3 data/db/market_signals.db "DELETE FROM market_signals WHERE timestamp < date('now', '-30 days');"

# 3. 优化数据库
sqlite3 data/db/market_signals.db "VACUUM;"

# 4. 分析表结构
sqlite3 data/db/market_signals.db "ANALYZE;"
```

---

## 📊 数据统计

### 查看数据库信息

```bash
# 文件大小
du -h data/db/*.db

# 记录数量
sqlite3 data/db/market_signals.db "SELECT COUNT(*) FROM market_signals;"

# 按币种统计
sqlite3 data/db/market_signals.db "SELECT symbol, COUNT(*) FROM market_signals GROUP BY symbol;"

# 按日期统计
sqlite3 data/db/market_signals.db "SELECT DATE(timestamp), COUNT(*) FROM market_signals GROUP BY DATE(timestamp);"
```

### 数据库表结构

#### **market_signals 表**

```sql
CREATE TABLE market_signals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp DATETIME NOT NULL,
    symbol VARCHAR(10) NOT NULL,
    trade_action VARCHAR(10) NOT NULL,  -- LONG/SHORT/NO_TRADE
    state_reason TEXT,
    system_state VARCHAR(20),           -- v3.0新增
    market_regime VARCHAR(20),          -- v3.0新增
    price FLOAT,
    price_change_24h FLOAT,
    price_trend_6h FLOAT,
    volume_change_6h FLOAT,
    oi_change_6h FLOAT,
    funding_rate FLOAT,
    buy_ratio_1h FLOAT,
    sell_ratio_1h FLOAT,
    total_amount_1h FLOAT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### **system_state 表**

```sql
CREATE TABLE system_state (
    symbol VARCHAR(10) PRIMARY KEY,
    system_state VARCHAR(20) NOT NULL,
    market_regime VARCHAR(20) NOT NULL,
    cooldown_counter INTEGER DEFAULT 0,
    last_decision VARCHAR(20),
    state_entry_time DATETIME,
    last_update_time DATETIME,
    state_history TEXT,  -- JSON格式
    extra_data TEXT      -- JSON格式
);
```

---

## 🎯 最佳实践

### 1. 定期备份
```bash
# 每天自动备份
0 2 * * * /path/to/backup_db.sh
```

### 2. 数据清理
```bash
# 每周清理30天前的旧数据
0 3 * * 0 sqlite3 /path/to/data/db/market_signals.db "DELETE FROM market_signals WHERE timestamp < date('now', '-30 days');"
```

### 3. 性能优化
```bash
# 每月优化一次数据库
0 4 1 * * sqlite3 /path/to/data/db/market_signals.db "VACUUM; ANALYZE;"
```

### 4. 监控磁盘空间
```bash
# 检查数据目录大小
du -sh data/db/

# 设置告警（磁盘使用超过80%）
df -h | awk '$5 > 80 {print "WARNING: Disk usage high"}'
```

### 5. 权限管理
```bash
# 确保正确的文件权限
chmod 755 data/db/
chmod 644 data/db/*.db
```

---

## 📚 相关文档

- [Docker使用说明](Docker使用说明.md)
- [历史信号记录使用说明](历史信号记录使用说明.md)
- [市场分析v3.0状态机说明](市场分析v3.0状态机说明.md)
- [当前市场分析技术总结](当前市场分析技术总结.md)

---

## ✅ 总结

### 配置清单

- [x] Docker volume挂载：`./data/db:/app/db`
- [x] Python代码配置：使用 `DB_DIR` 环境变量
- [x] 目录结构：`data/db/` 创建并添加到Git
- [x] .gitignore：忽略 `*.db` 文件
- [x] 测试验证：数据库文件成功创建在宿主机

### 优势总结

| 特性 | 说明 |
|------|------|
| 🔒 **数据安全** | 容器删除不影响数据 |
| 💾 **易于备份** | 直接复制文件即可 |
| 🔍 **便于分析** | 可直接访问数据库 |
| 🚀 **快速恢复** | 从备份快速恢复 |
| 📊 **数据迁移** | 复制文件即可迁移 |

---

**最后更新：2026-01-20**  
**版本：v3.0+**  
**状态：✅ 已生产验证**
